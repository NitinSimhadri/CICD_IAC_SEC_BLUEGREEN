name: CI/CD Pipeline - Blue Green Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

    # ✅ 1. Checkout Code
    - name: Checkout Code
      uses: actions/checkout@v3

    # ✅ 2. Configure AWS Credentials
    - name: Configure AWS
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

    # 🔥 3. SonarQube Scan
    - name: SonarQube Scan
      run: |
        cd app
        mvn clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
        -Dsonar.projectKey=CICD_IAC_SEC_BLUEGREEN \
        -Dsonar.projectName='CICD_IAC_SEC_BLUEGREEN' \
        -Dsonar.host.url=http://<YOUR-SONAR-IP>:9000 \
        -Dsonar.login=${{ secrets.SONAR_TOKEN }}

    # 🔥 4. Build Docker Image
    - name: Build Docker Image
      run: docker build -t app ./app

    # 🔥 5. Install Trivy (stable way)
    - name: Install Trivy
      run: |
        sudo apt-get update
        sudo apt-get install -y wget apt-transport-https gnupg lsb-release
        wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
        echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee -a /etc/apt/sources.list.d/trivy.list
        sudo apt-get update
        sudo apt-get install -y trivy

    # 🔥 6. Scan Docker Image
    - name: Trivy Scan
      run: trivy image app

    # 🔥 7. Login to AWS ECR
    - name: Login to ECR
      run: |
        aws ecr get-login-password --region ap-south-1 | \
        docker login --username AWS --password-stdin ${{ secrets.ECR_URL }}

    # 🔥 8. Push Image to ECR
    - name: Push Image
      run: |
        docker tag app:latest ${{ secrets.ECR_URL }}:latest
        docker push ${{ secrets.ECR_URL }}:latest

    # 🔵🟢 9. Decide Blue/Green Target
    - name: Decide Deployment Target
      id: target
      run: |
        CURRENT=$(aws elbv2 describe-listeners \
          --load-balancer-arn ${{ secrets.ALB_ARN }} \
          --query "Listeners[0].DefaultActions[0].TargetGroupArn" \
          --output text)

        if [[ $CURRENT == *"blue"* ]]; then
          echo "env=green" >> $GITHUB_OUTPUT
        else
          echo "env=blue" >> $GITHUB_OUTPUT
        fi

    # 🚀 10. Deploy to EC2 using SSM
    - name: Deploy via SSM
      run: |
        INSTANCE_ID=$(aws ec2 describe-instances \
          --filters "Name=tag:Name,Values=${{ steps.target.outputs.env }}-instance" \
          --query "Reservations[*].Instances[*].InstanceId" \
          --output text)

        aws ssm send-command \
          --instance-ids $INSTANCE_ID \
          --document-name "AWS-RunShellScript" \
          --parameters commands="[
            \"aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin ${{ secrets.ECR_URL }}\",
            \"docker pull ${{ secrets.ECR_URL }}:latest\",
            \"docker stop app || true\",
            \"docker rm app || true\",
            \"docker run -d -p 8080:8080 --name app ${{ secrets.ECR_URL }}:latest\"
          ]"

    # 🔁 11. Switch ALB Traffic
    - name: Switch Traffic
      run: |
        TG_ARN=$(aws elbv2 describe-target-groups \
          --query "TargetGroups[?contains(TargetGroupName, '${{ steps.target.outputs.env }}')].TargetGroupArn" \
          --output text)

        LISTENER_ARN=$(aws elbv2 describe-listeners \
          --load-balancer-arn ${{ secrets.ALB_ARN }} \
          --query "Listeners[0].ListenerArn" \
          --output text)

        aws elbv2 modify-listener \
          --listener-arn $LISTENER_ARN \
          --default-actions Type=forward,TargetGroupArn=$TG_ARN
